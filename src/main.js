/*import $ from 'jquery';
import { Numeral } from './entries';
import 'bootstrap/dist/css/bootstrap.min.css';
import './styles.css';

$(document).ready(function() {
	$("#inputForm").submit(function() {
		var num = new Numeral();
		num.translate($("#input").val());
		$("#output #number").text(num.log + " " + num.num);
		$("#output").show()
		event.preventDefault();
	});
});*/
